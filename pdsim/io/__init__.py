"""Result persistence: run folders with config + seed + time series.

One folder per run — ``runs/<timestamp>_<slug>/`` — per ``docs/DESIGN.md`` §8.
Headless (hard rule 4): no UI or plotting imports. Lands in milestone 5.
"""
