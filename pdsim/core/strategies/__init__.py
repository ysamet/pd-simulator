"""Strategy implementations — one module per strategy (``docs/DESIGN.md`` §2.3).

Each strategy registers itself with the strategy registry so the UI discovers
it automatically, and registers its tunable parameters with the Parameter
Registry. Strategies arrive in milestone 3 (``docs/ROADMAP.md``).
"""
