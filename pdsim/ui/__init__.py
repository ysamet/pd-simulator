"""Streamlit UI: registry-generated parameter panel + live charts.

A thin, replaceable layer over the headless engine (``docs/DESIGN.md`` §1.2):
it only builds an ``ExperimentConfig`` and renders event streams. Lands in
milestone 6.
"""
